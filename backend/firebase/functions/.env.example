# Default-off server control. Set to the exact string "true" only after documented
# human approval for the intended environment. Never put secret values in this file.
KARRI_PUSH_DELIVERY_ENABLED=false
